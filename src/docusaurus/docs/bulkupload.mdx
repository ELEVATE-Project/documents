import Admonition from '@theme/Admonition';

# Uploading Users to the Application

If an organization consists of a large number of users whose roles (mentor and mentee) are already assigned, the organization administrator can upload the list of users to the application using a CSV file.

**To upload users to the application, do as follows:**

1. Go to the <b>Application</b> menu ![burger menu](media/burgermenu-icon.png) and select **Admin Workspace**.

2. On the Admin Workspace page, click **Manage user**.

3. On the **Bulk Upload** tab, click **Download Sample CSV** to download the sample CSV file.

4. Open the downloaded CSV file and clear the sample details on the file.

    ![](media/orgadmin-bulkupload.png)

5. Enter the following user information to the file:

    * Email ID
    * Assigned role
 
6. On the **Bulk Upload** tab, click **Upload CSV** and upload the file containing the list of users. The user entries are successfully uploaded.

    <Admonition type="tip">
    <p>You will receive an email notification containing a CSV file with the following information about the list of users:</p>
    <ul>
    <li>Assigned user ID</li>
    <li>Whether the user uploaded to the portal already exists</li>
    </ul>
    </Admonition>